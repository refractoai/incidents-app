// Auto-generated by generate-schema-json.sh
const cds  = require('@sap/cds');
const fs   = require('fs');     // core module
const path = require('path');   // core module / fallback npm pkg

// Path to schema.cds (same folder as this script)
const filePath = path.join(__dirname, 'schema.cds');
const fileContent = fs.readFileSync(filePath, 'utf8');

// Parse & compile to CSN-JSON
const csn  = cds.parse.cdl(fileContent);
const json = cds.compile.to.json(csn);
console.log(json)
